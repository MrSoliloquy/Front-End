// Diferencias entre un Método y una Función

const numero1 = 20;
const numero2 = "20";

// FUNCIÓN (seguida de paréntesis)
console.log(parseInt(numero2)); // Transforma un string, si es válido, a un int

// MÉTODO (seguido de punto)
console.log(numero1.toString()); // Transforma a string